[tool.uv.workspace]
members = [
    "packages/ocp-server",
    "packages/ocp-client",
    "packages/ocp-conformance",
    "packages/ocp-router",
    "packages/opencontextprotocol-sdk",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23,<1.0",
    "ruff>=0.4",
    "mypy>=1.10",
]
